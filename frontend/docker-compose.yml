services:
  calendo-fe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: calendo-fe
    restart: always
    ports:
      - "3002:80"
    networks:
      - calendo-network

networks:
  calendo-network:
    driver: bridge
